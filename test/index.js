var expect = require('chai').expect;
var main = require('..');

describe('email test', function () {
    it('send registration email ', function () {
        expect([1, 2, 3].indexOf(4)).to.be.equal(-1); // 4 is not present in this array so indexOf returns -1
    });
});
